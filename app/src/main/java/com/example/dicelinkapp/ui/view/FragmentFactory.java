package com.example.dicelinkapp.ui.view;
import androidx.fragment.app.Fragment;

public interface FragmentFactory {
    // Method signature for creating a Fragment instance
    Fragment createFragment();
}
